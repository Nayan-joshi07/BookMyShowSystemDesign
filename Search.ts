import { Movie } from "./Movie";
export class Search {
    searchMoviesByNames(name: string): Array<Movie> {
        return [];
    }

    searchMoviesByGenre(name: string): Array<Movie> {
        return [];
    }

    searchMoviesByLanguage(language: string): Array<Movie> {
        return [];
    }

    searchMoviesByDate(releaseDate: Date): Array<Movie> {
        return [];
    }
}
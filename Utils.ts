export enum SeatType {
    DELUX = 0,
    ECONOMY = 1,
    VIP = 2,
    OTHER = 3
}

export enum SeatStatus {
    AVAILABLE = 0,
    NOAVAILABLE = 1,
    BOOKED = 2,
    RESERVED = 3
}

export enum Genre {
    SCI_FI = 0,
    DRAMA = 1,
    ROM_COM = 2,
    FANTASY = 3
}